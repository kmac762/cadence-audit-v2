# RC10 - Content Freshness

Adds a sample-based Content Freshness module for article pages. Age alone is never a finding. Candidates require additional staleness signals such as outdated years in page messaging, older references or dated source URLs. The module also surfaces title/topic overlap as a manual consolidation review cue.

The module cannot prove traffic decay without historical performance data such as Search Console.
